streamlit>=1.38
faster-whisper>=1.0
ollama>=0.3
markdown>=3.6
fpdf2>=2.7
python-dotenv>=1.0
# Importing existing notes from other formats. Image OCR also needs the
# Tesseract engine itself, which the Docker image installs.
pypdf>=4.0
python-docx>=1.1
pytesseract>=0.3.10
Pillow>=10.0
# Reading LMS calendar (ICS) feeds for assignment deadlines
icalendar>=5.0
pytest>=8.0
